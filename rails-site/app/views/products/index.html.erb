<h1>Listing products</h1>

<table>
    <tr>
        <th>ID</th>
        <th>Description</th>
        <th colspan="1"></th>
    </tr>

    <% @products.each do |product| %>
        <tr>
            <td><%= product.product_id %></td>
            <td><%= product.description %></td>
            <td><%= link_to 'Show', product_path(product.product_id) %></td>
        </tr>
    <% end %>
</table>
