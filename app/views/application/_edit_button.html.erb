<% if params[:edit] == 'true' %>
  <%= link_to 'Finished editing', params.merge(:edit => false), {:class => 'btn btn-primary'} %>
<% else %>
  <%= link_to 'Edit', params.merge(:edit => true), {:class => 'btn btn-primary'} %>
<% end %>
