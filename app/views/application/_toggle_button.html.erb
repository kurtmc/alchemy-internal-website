<% if params[option] == 'true' %>
  <%= link_to toggle_off, params.merge(option => false), {:class => 'btn btn-primary'} %>
<% else %>
  <%= link_to toggle_on, params.merge(option => true), {:class => 'btn btn-primary'} %>
<% end %>
